export const getNewComment = state => state.newComment.newComment;

export const isNewCommentErrored = state => state.newComment.error;

export const isNewCommentLoading = state => state.newComment.loading;
